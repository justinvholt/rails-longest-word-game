<h1>Let's Play!</h1>
<p>Submit the longest English word you can find in the letters.</p>

<div class="game">
  <ul id="letters_list">
    <% @letters.each do |letter| %>
    <li><%= letter %></li>
    <% end %>
  </ul>
</div>

<form action="/score" class="game" method="post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <input type="text" id="text_field" name="word" placeholder="Longest word">
  <input type="submit">
</form>
